# Vercel falls back to .gitignore when this file is absent. The bear photos are
# committed now, so nothing here is load-bearing -- this file just keeps the
# upload tidy and independent of whatever .gitignore grows later.

node_modules
dist
.env
.env.local
*.log
