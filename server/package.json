{
  "name": "fat-bear-week-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend for the Fat Bear Week bracket voting party app",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^5.0.1"
  }
}
